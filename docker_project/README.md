# docker-project
## http://localhost:8090/weather/{CityName} 

## for migrate up:
- migrate -path db/migration -database {databse_url} -verbose up

## for migrate down:

- migrate -path db/migration -database {databse_url} -verbose up
